'use strict';

const config = {
  host: 'https://auth.commercetools.co',
  projectKey: '',
  credentials: {
    clientId: '',
    clientSecret: '',
  },
}

const host = {
  host: 'https://api.commercetools.co',
  }

module.exports = {config, host};
