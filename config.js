'use strict';

const config = {
  host: 'https://auth.commercetools.co',
  projectKey: '',
  credentials: {
    clientId: '',
    clientSecret: '',
  },
}

const host = {
  host: '',
  }

module.exports = {config, host};
